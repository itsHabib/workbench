---
driver_version: 1
generated_at: 2026-08-04T13:50:00Z
generated_by: work-driver-prep
source:
  project: gate
  phase: tier-aware-panel-p1-trust-path
repo: workbench
repo_url: https://github.com/itsHabib/workbench
branch_prefix: tier-aware-panel-
default_runtime: local

batches:
  - id: 1
    label: ready now
    depends_on: []
    status: done
    completed_at: 2026-08-04T18:16:09.811Z
    streams:
      - task_id: tsk_01KZ2QRQEVRJNHKFC1MEN439DS
        task_slug: pin-panel-declaration-default-branch
        # The task IS the spec — read it with mcp__dossier__task_get, INCLUDING
        # its notes (they carry the gh-seam finding and the full-argv
        # refinement). spec_path points at the governing design section for
        # context, not at a per-task spec doc; there deliberately isn't one.
        spec_path: docs/features/tier-aware-panel/spec.md
        spec_section: "§7.2"
        branch_name: tier-aware-panel-pin-declaration-default-branch
        runtime: local
        model: sonnet
        effort: extra
        touches:
          - cmd/gate/internal/evidence/panel.go
          - cmd/gate/internal/evidence/panel_test.go
        # Cursor was deferred by the operator; the same isolated-worktree change
        # landed through PR #216 and Gate run_36fdc0f7954afaec.
        status: skipped

conflict_notes:
  - kind: none
    note: "Single-task phase — nothing to batch. Cross-phase ordering is real but outside this manifest: P2 (thread-floor-tier-into-panel) depends on this task; P3 additionally blocked on TDD §10.7."

skipped_during_resolution: []
---

# P1 — pin the panel-declaration trust path · driver manifest

Generated by `/work-driver-prep project:gate:phase:tier-aware-panel-p1-trust-path`.
Consumed by `/work-driver docs/features/tier-aware-panel/driver.md`.

## The task

`tsk_01KZ2QRQEVRJNHKFC1MEN439DS` — *Pin that the panel declaration is read from
the default branch, not the PR head.*

**Read it from dossier** (`mcp__dossier__task_get`), notes included. There is no
per-task spec doc on purpose: the task already carries problem / fix /
acceptance / test plan, and its notes carry two things resolved in advance —

- **The seam.** `evidence.gh` is a package-level func calling `exec.Command`
  directly; nothing is injectable. `exec.Command` resolves `gh` through `PATH`,
  so a fake `gh` binary observes the argv with zero production change.
- **The assertion.** Cover the full argument vector, not the URL string —
  `gh api` can select a ref via `?ref=`, `-f`, `-F`, `--field`, `--raw-field`.

## Batch

```
Batch 1 — 1 stream:
  - pin-panel-declaration-default-branch
      touches: cmd/gate/internal/evidence/{panel.go, panel_test.go}
      runtime: local · model: sonnet · effort: extra
```

## Why this phase runs first

The tier-aware panel design rests on the declaration being read from the default
branch rather than the PR head — that is what stops a PR from lowering the review
bar it will be judged against. Today the property holds only because a parameter
was omitted; nothing asserts it. Pinning it before P2 threads the tier and P3
adds the policy is the point of the ordering.
