---
driver_version: 1
generated_at: 2026-07-15T19:15:00Z
generated_by: work-driver-prep
source:
  project: workbench
  phase: dispatch-decide-core
repo: workbench
repo_url: https://github.com/itsHabib/workbench
branch_prefix: dispatch-
default_runtime: local

batches:
  - id: 1
    label: ready now — decide core
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KXKC4YEPR094Z1SG05AQTC9K
        task_slug: dispatch-decide-core
        spec_path: docs/features/dispatch/dispatch-decide-core.md
        branch_name: dispatch-decide-core
        runtime: local
        model: opus
        effort: max
        touches:
          - cmd/dispatch/
        status: pending

conflict_notes:
  - kind: single_stream
    note: "Sole task in phase dispatch-decide-core. The replay-gate task lives in phase dispatch-replay-validation and depends_on this one, so it is correctly excluded from this manifest — it preps after this PR merges."

skipped_during_resolution: []
---

# dispatch decide core — driver manifest

Generated by `/work-driver-prep project:workbench:phase:dispatch-decide-core` on
2026-07-15. Consumed by `/work-driver docs/features/dispatch/driver.md`.

## Batches

### Batch 1 — ready now (1 stream)

| Stream | Spec | Runtime | Model/effort | Touches |
|---|---|---|---|---|
| `dispatch-decide-core` | docs/features/dispatch/dispatch-decide-core.md | local | opus/max | `cmd/dispatch/` |

Phase 1 of the dispatch TDD (`docs/features/dispatch/spec.md`, merged #37): the
`decide` + `validate` verbs, the fail-closed exit-code contract, and the frozen
`task_class` taxonomy. ~600 weighted (ideal band). Correctness-critical → opus/max.

**Runtime = local** (workbench prep default): single repo, stdlib-only Go,
L1/L2 unit-test-driven — the operator can step into the run with normal Go
tooling. Cloud alternative available (flip `runtime: cloud` + drop the
docs-PR-merge gate below, since cloud embeds the spec content from disk); local
was chosen by the heuristic, override if you'd rather dogfood the cloud path.

**Next phase**: once this merges, `/work-driver-prep
project:workbench:phase:dispatch-replay-validation` preps the validation gate.
