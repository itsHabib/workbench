{
  "name": "console-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright browser e2e for cmd/console, driving the real console binary against committed gate-state fixtures. Test-only; confined to this directory, never part of the Go module or production build.",
  "type": "module",
  "scripts": {
    "gen-fixtures": "node scripts/gen-fixtures.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
