# Codex discovery pointer

Read `CLAUDE.md` in this directory, then `docs/DESIGN.md`, before changing
codexguard. Those files define the public exit-code seam, the supported
envelopes, and the exact-head merge invariants.
