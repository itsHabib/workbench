# Codex discovery pointer

Read and follow the adjacent `CLAUDE.md`, then `docs/DESIGN.md`, before
changing codexguard. Those files define the public exit-code seam, the
supported envelopes, and the exact-head merge invariants.
